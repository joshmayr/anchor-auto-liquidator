import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../..';
declare type AssetInfo = {
    token: {
        contract_addr: string;
    };
} | {
    native_token: {
        denom: string;
    };
};
interface Option {
    lcd: LCDClient;
    pair_contract_address: string;
}
interface PairInfo {
    assetInfos: AssetInfo[];
    contractAddr: string;
    liquidityToken: string;
}
export declare const queryExchangePair: ({ lcd, pair_contract_address }: Option) => (_: AddressProvider) => Promise<PairInfo>;
export {};
