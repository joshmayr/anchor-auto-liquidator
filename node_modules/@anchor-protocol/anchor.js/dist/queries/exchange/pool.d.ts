import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../..';
interface Option {
    lcd: LCDClient;
    pair_contract_address: string;
}
interface PoolResponse {
    assets: Asset[];
    total_share: string;
}
interface Asset {
    info: string;
    amount: string;
}
export declare const queryExchangePool: ({ lcd, pair_contract_address }: Option) => (_: AddressProvider) => Promise<PoolResponse>;
export {};
