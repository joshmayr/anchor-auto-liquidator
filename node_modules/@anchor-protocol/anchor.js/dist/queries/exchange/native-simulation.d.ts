import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../..';
interface Option {
    lcd: LCDClient;
    denom: string;
    amount: string;
    pair_contract_address: string;
}
interface SimulationResponse {
    return_amount: string;
    spread_amount: string;
    commission_amount: string;
}
export declare const queryExchangeNativeSimulation: ({ lcd, denom, amount, pair_contract_address }: Option) => (_: AddressProvider) => Promise<SimulationResponse>;
export {};
