import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { Holder } from '..';
interface Option {
    lcd: LCDClient;
    address: string;
}
export declare const querybEthRewardHolder: ({ lcd, address }: Option) => (addressProvider: AddressProvider) => Promise<Holder>;
export {};
