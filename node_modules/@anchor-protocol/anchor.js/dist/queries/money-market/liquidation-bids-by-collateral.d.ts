import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { BidResponse } from '../../queries';
interface Option {
    lcd: LCDClient;
    collateral_token: string;
    start_after?: string;
    limit?: number;
}
interface BidsByCollateralResponse {
    bids: BidResponse[];
}
export declare const queryLiquidationBidsByCollateral: ({ lcd, collateral_token, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<BidsByCollateralResponse>;
export {};
