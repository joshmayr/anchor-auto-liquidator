import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    market: MARKET_DENOMS;
    borrower: string;
    block_height: number;
}
export interface BorrowInfoResponse {
    borrower: string;
    interest_index: string;
    reward_index: string;
    loan_amount: string;
    pending_rewards: string;
}
export declare const queryMarketBorrowerInfo: ({ lcd, market, borrower, block_height }: Option) => (addressProvider: AddressProvider) => Promise<BorrowInfoResponse>;
export {};
