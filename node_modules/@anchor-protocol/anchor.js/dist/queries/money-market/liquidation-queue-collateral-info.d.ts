import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    collateral_token: string;
}
interface CollateralInfoResponse {
    collateral_token: string;
    bid_threshold: string;
    max_slot: number;
    premium_rate_per_slot: string;
}
export declare const queryLiquidationQueueCollateralInfo: ({ lcd, collateral_token }: Option) => (addressProvider: AddressProvider) => Promise<CollateralInfoResponse>;
export {};
