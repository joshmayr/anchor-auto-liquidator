import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { BidResponse } from '../../queries';
interface Option {
    lcd: LCDClient;
    bidder: string;
    start_after?: string;
    limit?: number;
}
interface BidsByUserResponse {
    bids: BidResponse[];
}
export declare const queryLiquidationBidsByUser: ({ lcd, bidder, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<BidsByUserResponse>;
export {};
