import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    market_balance: string;
    total_liabilities: string;
    total_reserves: string;
}
interface BorrowRateResponse {
    rate: string;
}
export declare const queryInterestModelBorrowRate: ({ lcd, market_balance, total_liabilities, total_reserves }: Option) => (addressProvider: AddressProvider) => Promise<BorrowRateResponse>;
export {};
