import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    borrow_amount: string;
    borrow_limit: string;
    collaterals: Array<[string, string]>;
    collateral_prices: string[];
}
interface LiquidationAmountResponse {
    collaterals: Array<[string, string]>;
}
export declare const queryLiquidationLiquidationAmount: ({ lcd, borrow_amount, borrow_limit, collaterals, collateral_prices, }: Option) => (addressProvider: AddressProvider) => Promise<LiquidationAmountResponse>;
export {};
