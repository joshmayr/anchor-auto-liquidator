import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    overseer: MARKET_DENOMS;
    borrower: string;
    block_time?: number;
}
interface BorrowLimitResponse {
    borrower: string;
    borrow_limit: string;
}
export declare const queryOverseerBorrowLimit: ({ lcd, overseer, borrower, block_time }: Option) => (addressProvider: AddressProvider) => Promise<BorrowLimitResponse>;
export {};
