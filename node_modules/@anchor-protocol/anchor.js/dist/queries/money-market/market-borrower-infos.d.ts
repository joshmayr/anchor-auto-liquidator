import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
import { BorrowInfoResponse } from '../../queries';
interface Option {
    lcd: LCDClient;
    market: MARKET_DENOMS;
    start_after?: string;
    limit?: number;
}
interface BorrowerInfosResponse {
    borrower_infos: BorrowInfoResponse[];
}
export declare const queryMarketBorrowerInfos: ({ lcd, market, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<BorrowerInfosResponse>;
export {};
