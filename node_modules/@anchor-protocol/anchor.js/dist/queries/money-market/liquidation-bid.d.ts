import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    collateral_token: string;
    bidder: string;
}
export interface BidResponse {
    collateral_token: string;
    bidder: string;
    amount: string;
    premium_rate: string;
}
export declare const queryLiquidationBid: ({ lcd, collateral_token, bidder }: Option) => (addressProvider: AddressProvider) => Promise<BidResponse>;
export {};
