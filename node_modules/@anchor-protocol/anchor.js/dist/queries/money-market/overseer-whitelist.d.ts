import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    market: MARKET_DENOMS;
    collateral_token?: string;
    start_after?: string;
    limit?: number;
}
interface WhitelistResponse {
    elems: WhitelistResponseElem[];
}
interface WhitelistResponseElem {
    name: string;
    symbol: string;
    max_ltv: string;
    custody_contract: string;
    collateral_token: string;
}
export declare const queryOverseerWhitelist: ({ lcd, market, collateral_token, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<WhitelistResponse>;
export {};
