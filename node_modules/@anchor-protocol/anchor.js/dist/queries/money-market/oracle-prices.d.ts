import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    start_after?: string;
    limit?: number;
}
export interface PricesResponse {
    prices: PricesResponseElem[];
}
interface PricesResponseElem {
    asset: string;
    price: string;
    last_update_time: number;
}
export declare const queryOraclePrices: ({ lcd, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<PricesResponse>;
export {};
