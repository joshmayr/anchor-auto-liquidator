import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    overseer: MARKET_DENOMS;
    start_after?: string;
    limit?: number;
}
interface AllCollateralsResponse {
    all_collaterals: CollateralsResponse[];
}
interface CollateralsResponse {
    borrower: string;
    collaterals: Array<[string, string]>;
}
export declare const queryOverseerAllCollaterals: ({ lcd, overseer, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<AllCollateralsResponse>;
export {};
