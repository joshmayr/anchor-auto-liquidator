import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider, COLLATERAL_DENOMS, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    market: MARKET_DENOMS;
    custody: COLLATERAL_DENOMS | string;
    address: string;
}
export interface BorrowerResponse {
    borrower: string;
    balance: string;
    spendable: string;
}
export declare const queryCustodyBorrower: ({ lcd, market, custody, address }: Option) => (addressProvider: AddressProvider) => Promise<BorrowerResponse>;
export {};
