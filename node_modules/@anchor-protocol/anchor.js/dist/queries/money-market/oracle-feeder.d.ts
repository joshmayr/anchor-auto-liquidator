import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    asset: string;
}
interface FeederResponse {
    asset: string;
    feeder: string;
}
export declare const queryOracleFeeder: ({ lcd, asset }: Option) => (addressProvider: AddressProvider) => Promise<FeederResponse>;
export {};
