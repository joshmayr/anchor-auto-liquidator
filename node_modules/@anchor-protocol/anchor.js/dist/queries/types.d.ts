import { AddressProvider } from '..';
export declare type QueryOptionType<T> = T extends Querier<infer Option, unknown> ? Option : null;
export declare type QueryResponseType<T> = T extends Querier<unknown, infer Response> ? Promise<Response> : Promise<unknown>;
export declare type Querier<T, R> = (option: T) => (addressProvider: AddressProvider) => Promise<R>;
