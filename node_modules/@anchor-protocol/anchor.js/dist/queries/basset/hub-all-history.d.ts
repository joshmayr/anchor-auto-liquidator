import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    start_from?: number;
    limit?: number;
}
interface HistoryResponse {
    history: UnbondHistory[];
}
interface UnbondHistory {
    batch_id: number;
    time: number;
    amount: string;
    bluna_amount: string;
    stluna_amount: string;
    applied_exchange_rate: string;
    withdraw_rate: string;
    bluna_withdraw_rate: string;
    stluna_withdraw_rate: string;
    released: boolean;
}
export declare const queryHubHistory: ({ lcd, start_from, limit }: Option) => (addressProvider: AddressProvider) => Promise<HistoryResponse>;
export {};
