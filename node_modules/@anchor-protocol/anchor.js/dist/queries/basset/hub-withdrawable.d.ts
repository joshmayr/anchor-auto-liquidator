import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider';
interface Option {
    lcd: LCDClient;
    address: string;
    block_time: number;
}
interface WithdrableResponse {
    withdrawable: string;
}
export declare const queryHubWithdrawable: ({ lcd, address, block_time }: Option) => (addressProvider: AddressProvider) => Promise<WithdrableResponse>;
export {};
