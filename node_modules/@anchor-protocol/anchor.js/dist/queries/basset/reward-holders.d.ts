import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { Holder } from '../../queries';
interface Option {
    lcd: LCDClient;
    start_after?: string;
    limit?: number;
}
interface Holders {
    holders: Holder[];
}
export declare const queryRewardHolders: ({ lcd, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<Holders>;
export {};
