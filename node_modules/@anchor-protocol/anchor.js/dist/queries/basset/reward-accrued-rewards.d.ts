import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    address: string;
}
interface AccruedReward {
    rewards: string;
}
export declare const queryRewardAccrued: ({ lcd, address }: Option) => (addressProvider: AddressProvider) => Promise<AccruedReward>;
export {};
