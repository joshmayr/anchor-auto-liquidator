import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
}
interface CurrentBatchResponse {
    id: number;
    requested_with_fee: string;
    requested_bluna_with_fee: string;
    requested_stluna: string;
}
export declare const queryHubCurrentBatch: ({ lcd }: Option) => (addressProvider: AddressProvider) => Promise<CurrentBatchResponse>;
export {};
