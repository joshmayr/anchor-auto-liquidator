import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider';
interface Option {
    lcd: LCDClient;
    address: string;
}
export interface UnbondResponse {
    address: string;
    requests: Array<[number, string, string]>;
}
export declare const queryHubUnbond: ({ lcd, address }: Option) => (addressProvider: AddressProvider) => Promise<UnbondResponse>;
export {};
