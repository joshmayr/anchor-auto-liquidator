import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider';
interface Option {
    lcd: LCDClient;
}
interface StateResponse {
    global_index: string;
    total_balance: string;
    prev_reward_balance: string;
}
export declare const queryRewardState: ({ lcd }: Option) => (addressProvider: AddressProvider) => Promise<StateResponse>;
export {};
