import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider';
interface Option {
    lcd: LCDClient;
}
interface WhitelistedValResponse {
    validators: string[];
}
export declare const queryHubWhiteVals: ({ lcd }: Option) => (addressProvider: AddressProvider) => Promise<WhitelistedValResponse>;
export {};
