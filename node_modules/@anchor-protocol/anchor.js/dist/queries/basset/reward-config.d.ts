import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider';
interface Option {
    lcd: LCDClient;
}
interface ConfigResponse {
    hub_contract: string;
    reward_denom: string;
}
export declare const queryRewardConfig: ({ lcd }: Option) => (addressProvider: AddressProvider) => Promise<ConfigResponse>;
export {};
