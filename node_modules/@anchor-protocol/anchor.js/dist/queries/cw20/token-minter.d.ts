import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    token_address: string;
}
interface MinterResponse {
    minter: string;
    cap?: string;
}
export declare const queryTokenMinter: ({ lcd, token_address }: Option) => (_: AddressProvider) => Promise<MinterResponse>;
export {};
