import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { Expire } from '../../fabricators';
interface Option {
    lcd: LCDClient;
    owner: string;
    token_address: string;
    start_after?: string;
    lim?: number;
}
interface AllAllowance {
    allowances: AllowanceResponse[];
}
interface AllowanceResponse {
    allowance: string;
    expires: Expire;
}
export declare const queryTokenAllowances: ({ lcd, owner, token_address, start_after, lim }: Option) => (_: AddressProvider) => Promise<AllAllowance>;
export {};
