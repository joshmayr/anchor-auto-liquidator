import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    address: string;
    token_address: string;
}
interface Balance {
    balance: string;
}
export declare const queryTokenBalance: ({ lcd, address, token_address }: Option) => (_: AddressProvider) => Promise<Balance>;
export {};
