import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider';
interface Option {
    lcd: LCDClient;
    token_address: string;
}
interface TokenInfoResponse {
    name: string;
    symbol: string;
    decimals: number;
    total_supply: number;
}
export declare const queryTokenInfo: ({ lcd, token_address }: Option) => (_: AddressProvider) => Promise<TokenInfoResponse>;
export {};
