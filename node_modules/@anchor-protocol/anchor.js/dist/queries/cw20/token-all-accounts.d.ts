import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    token_address: string;
    start_after?: string;
    limit?: number;
}
interface AllAccounts {
    accounts: string[];
}
export declare const queryTokenAllAccounts: ({ lcd, token_address, start_after, limit }: Option) => (_: AddressProvider) => Promise<AllAccounts>;
export {};
