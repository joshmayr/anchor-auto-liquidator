import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    staker: string;
    block_height?: number;
}
export interface StakerInfoResponse {
    staker: string;
    reward_index: string;
    bond_amount: string;
    pending_reward: string;
}
export declare const queryStakingStaker: ({ lcd, staker, block_height }: Option) => (addressProvider: AddressProvider) => Promise<StakerInfoResponse>;
export {};
