import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface ExecuteMsg {
    contract: string;
    msg: string;
}
export declare type PollStatus = 'in_progress' | 'passed' | 'rejected' | 'executed';
interface Option {
    lcd: LCDClient;
    poll_id: number;
}
export interface PollResponse {
    id: number;
    creator: string;
    status: PollStatus;
    end_height: number;
    title: string;
    description: string;
    link?: string;
    deposit_amount: string;
    execute_data?: ExecuteMsg;
    yes_votes: string;
    no_votes: string;
    staked_amount?: string;
    total_balance_at_end_poll?: string;
}
export declare const queryGovPoll: ({ lcd, poll_id }: Option) => (addressProvider: AddressProvider) => Promise<PollResponse>;
export {};
