import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    address: string;
}
declare type VoteOption = 'yes' | 'no';
export interface VoterInfo {
    vote: VoteOption;
    share: string;
}
export interface StakerResponse {
    balance: string;
    share: string;
    locked_share: Array<[number, VoterInfo]>;
}
export declare const queryGovStaker: ({ lcd, address }: Option) => (addressProvider: AddressProvider) => Promise<StakerResponse>;
export {};
