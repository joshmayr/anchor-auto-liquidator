import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
    poll_id: number;
    start_after?: string;
    limit?: number;
}
declare type VoteOption = 'yes' | 'no';
export interface VotersResponseItem {
    voter: string;
    vote: VoteOption;
    share: string;
    balance: string;
}
export interface VotersResponse {
    voters: Array<VotersResponseItem>;
}
export declare const queryGovVoters: ({ lcd, poll_id, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<VotersResponse>;
export {};
