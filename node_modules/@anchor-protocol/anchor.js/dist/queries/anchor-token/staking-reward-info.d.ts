import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
}
export interface RewardInfoResponse {
    staker: string;
    index: string;
    bond_amount: string;
    pending_reward: string;
}
export declare const queryStakingRewardInfo: ({ lcd }: Option) => (addressProvider: AddressProvider) => Promise<RewardInfoResponse>;
export {};
