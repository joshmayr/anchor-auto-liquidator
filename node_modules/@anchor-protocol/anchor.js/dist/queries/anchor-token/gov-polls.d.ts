import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { PollResponse, PollStatus } from './gov-poll';
interface Option {
    lcd: LCDClient;
    filter: PollStatus;
    start_after?: number;
    limit?: number;
}
interface PollsResponse {
    polls: PollResponse[];
}
export declare const queryGovPolls: ({ lcd, filter, start_after, limit }: Option) => (addressProvider: AddressProvider) => Promise<PollsResponse>;
export {};
