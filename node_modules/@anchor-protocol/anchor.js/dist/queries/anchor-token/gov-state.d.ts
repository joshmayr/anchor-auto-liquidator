import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    lcd: LCDClient;
}
interface StateResponse {
    poll_count: number;
    total_share: string;
    total_deposit: string;
}
export declare const queryGovState: ({ lcd }: Option) => (addressProvider: AddressProvider) => Promise<StateResponse>;
export {};
