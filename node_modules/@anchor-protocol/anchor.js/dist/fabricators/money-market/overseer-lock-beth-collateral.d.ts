import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    market: MARKET_DENOMS;
    amount: string;
}
/**
 *
 * @param address Client’s Terra address.
 * @param market Type of stablecoin money market to deposit collateral. Currently only supports UST.
 * @param amount Amount of collateral to deposit.
 */
export declare const fabricateOverseerLockBETHCollateral: ({ address, market, amount }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
