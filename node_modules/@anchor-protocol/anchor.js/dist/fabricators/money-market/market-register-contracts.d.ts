import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { MARKET_DENOMS } from '../../address-provider';
interface Option {
    address: string;
    market: MARKET_DENOMS;
    overseer_contract: string;
    interest_model: string;
    distribution_model: string;
    collector_contract: string;
    distributor_contract: string;
}
export declare const fabricatebMarketRegisterContracts: ({ address, overseer_contract, market, interest_model, distribution_model, collector_contract, distributor_contract, }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
