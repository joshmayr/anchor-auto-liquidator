import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, COLLATERAL_DENOMS, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    market: MARKET_DENOMS;
    collateral: COLLATERAL_DENOMS;
    owner?: string;
    liquidation_contract?: string;
}
export declare const fabricateCustodyUpdateConfig: ({ address, market, collateral, liquidation_contract, owner }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
