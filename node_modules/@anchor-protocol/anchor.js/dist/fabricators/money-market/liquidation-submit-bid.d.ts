import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    collateral_token: string;
    premium_rate: string;
    amount: string;
    denom: MARKET_DENOMS;
}
export declare const fabricateLiquidationSubmitBid: ({ address, collateral_token, premium_rate, amount, denom }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
