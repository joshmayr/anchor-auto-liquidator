import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    asset: string;
    feeder: string;
}
export declare const fabricateOracleRegisterFeeder: ({ address, asset, feeder }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
