import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { MARKET_DENOMS } from '../../address-provider';
interface Option {
    address: string;
    market: MARKET_DENOMS;
    owner_addr?: string;
    interest_model?: string;
    distribution_model?: string;
    reserve_factor?: string;
    max_borrow_factor?: string;
}
export declare const fabricateMarketUpdateConfig: ({ address, owner_addr, interest_model, distribution_model, reserve_factor, max_borrow_factor, market, }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
