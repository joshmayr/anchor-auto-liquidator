import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { MARKET_DENOMS } from '../../address-provider';
interface Option {
    address: string;
    market: MARKET_DENOMS;
    amount: string;
    to?: string;
}
/**
 *
 * @param address Client’s Terra address.
 * @param market Type of stablecoin money market to borrow.
 * @param amount Amount of stablecoin to borrow.
 */
export declare const fabricateMarketBorrow: ({ address, market, amount, to }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
