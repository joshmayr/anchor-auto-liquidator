import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    market: MARKET_DENOMS;
    amount: string;
}
/**
 *
 * @param address Client’s Terra address.
 * @param market Type of stablecoin money market to deposit collateral. Currently only supports UST and KRT.
 //  * @param borrower (optional) — Terra address of the entity that created the loan position. If null, adds collateral to address‘s loan position.
 * @param amount Amount of collateral to deposit.
 */
export declare const fabricateOverseerLockCollateral: ({ address, market, amount }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
