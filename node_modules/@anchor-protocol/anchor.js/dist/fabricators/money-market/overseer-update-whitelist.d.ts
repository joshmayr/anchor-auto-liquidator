import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    overseer: MARKET_DENOMS;
    collateral_token: string;
    custody_contract?: string;
    max_ltv?: string;
}
export declare const fabricateOverseerUpdateWhitelist: ({ address, overseer, collateral_token, custody_contract, max_ltv, }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
