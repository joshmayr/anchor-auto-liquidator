import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    overseer: MARKET_DENOMS;
    owner_addr?: string;
    oracle_contract?: string;
    liquidation_contract?: string;
    threshold_deposit_rate?: string;
    target_deposit_rate?: string;
    buffer_distribution_factor?: string;
    anc_purchase_factor?: string;
    epoch_period?: number;
    price_timeframe?: number;
}
export declare const fabricateOverseerUpdateConfig: ({ address, overseer, owner_addr, oracle_contract, liquidation_contract, threshold_deposit_rate, target_deposit_rate, buffer_distribution_factor, anc_purchase_factor, epoch_period, price_timeframe, }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
