import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    collateral_token: string;
    premium_slot: number;
    amount: string;
    denom: MARKET_DENOMS;
}
export declare const fabricateLiquidationQueueSubmitBid: ({ address, collateral_token, premium_slot, amount, denom }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
