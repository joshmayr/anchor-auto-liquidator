import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    overseer: MARKET_DENOMS;
    borrower: string;
}
export declare const fabricateOverseerLiquidateCollateral: ({ address, overseer, borrower }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
