import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    bid_idx: string;
    amount: string | undefined;
}
export declare const fabricateLiquidationQueueRetractBid: ({ address, bid_idx, amount }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
