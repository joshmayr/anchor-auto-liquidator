import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    owner?: string;
    base_rate?: string;
    interest_multiplier?: string;
}
export declare const fabricateInterestUpdateConfig: ({ address, owner, base_rate, interest_multiplier }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
