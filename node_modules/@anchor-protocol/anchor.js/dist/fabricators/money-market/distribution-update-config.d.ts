import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    owner: string;
    emission_cap?: string;
    emission_floor?: string;
    increment_multiplier?: string;
    decrement_multiplier?: string;
}
export declare const fabricateDistributionUpdateConfig: ({ address, owner, emission_cap, emission_floor, increment_multiplier, decrement_multiplier, }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
