"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fabricateMarketUpdateConfig = void 0;
const terra_js_1 = require("@terra-money/terra.js");
const validate_input_1 = require("../../utils/validate-input");
const address_1 = require("../../utils/validation/address");
const true_1 = require("../../utils/validation/true");
const number_1 = require("../../utils/validation/number");
const fabricateMarketUpdateConfig = ({ address, owner_addr, interest_model, distribution_model, reserve_factor, max_borrow_factor, market, }) => (addressProvider) => {
    (0, validate_input_1.validateInput)([
        (0, address_1.validateAddress)(address),
        owner_addr ? (0, address_1.validateAddress)(owner_addr) : true_1.validateTrue,
        interest_model ? (0, address_1.validateAddress)(interest_model) : true_1.validateTrue,
        reserve_factor ? (0, number_1.validateIsNumber)(reserve_factor) : true_1.validateTrue,
        max_borrow_factor ? (0, number_1.validateIsNumber)(max_borrow_factor) : true_1.validateTrue,
    ]);
    const mmMarket = addressProvider.market(market);
    return [
        new terra_js_1.MsgExecuteContract(address, mmMarket, {
            update_config: {
                owner_addr,
                interest_model,
                distribution_model,
                reserve_factor,
                max_borrow_factor,
            },
        }),
    ];
};
exports.fabricateMarketUpdateConfig = fabricateMarketUpdateConfig;
//# sourceMappingURL=market-update-config.js.map