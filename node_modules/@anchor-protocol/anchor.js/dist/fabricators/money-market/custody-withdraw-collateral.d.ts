import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, COLLATERAL_DENOMS, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    market: MARKET_DENOMS;
    collateral: COLLATERAL_DENOMS;
    amount?: string;
}
export declare const fabricateCustodyWithdrawCollateral: ({ address, market, collateral, amount }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
