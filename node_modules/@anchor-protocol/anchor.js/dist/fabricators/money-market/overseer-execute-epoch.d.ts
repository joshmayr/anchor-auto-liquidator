import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider/provider';
interface Option {
    address: string;
    overseer: MARKET_DENOMS;
}
export declare const fabricateOverseerEpochOperations: ({ address, overseer }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
