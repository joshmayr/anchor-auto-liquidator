import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    owner?: string;
}
export declare const fabricateOracleUpdateConfig: ({ address, owner }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
