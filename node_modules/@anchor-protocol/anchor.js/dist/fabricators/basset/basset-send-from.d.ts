import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    amount: string;
    owner: string;
    contract: string;
    msg?: object;
}
export declare const fabricatebAssetSendFrom: ({ address, amount, contract, owner, msg }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
