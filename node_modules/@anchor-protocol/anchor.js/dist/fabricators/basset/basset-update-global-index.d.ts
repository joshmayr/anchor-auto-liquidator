import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    airdrop_hooks?: string[];
}
export declare const fabricatebAssetUpdateGlobalIndex: ({ address, airdrop_hooks }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
