import { AddressProvider } from '../../address-provider/provider';
import { MsgExecuteContract } from '@terra-money/terra.js';
interface Option {
    address: string;
    recipient?: string;
}
export declare const fabricatebAssetClaimRewards: ({ address, recipient }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
