import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    owner?: string;
    reward_contract?: string;
    token_contract?: string;
    airdrop_registry_contract: string;
}
export declare const fabricatebAssetUpdateConfig: ({ address, owner, reward_contract, token_contract, airdrop_registry_contract, }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
