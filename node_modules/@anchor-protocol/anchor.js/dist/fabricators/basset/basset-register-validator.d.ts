import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    validator: string;
}
/**
 * @param address Client’s Terra address.
 */
export declare const fabricatebAssetRegisterValidator: ({ address, validator }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
