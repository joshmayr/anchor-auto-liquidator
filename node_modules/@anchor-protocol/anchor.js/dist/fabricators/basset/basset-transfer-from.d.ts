import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    amount: string;
    owner: string;
    recipient: string;
}
export declare const fabricatebAssetTransferFrom: ({ address, amount, owner, recipient }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
