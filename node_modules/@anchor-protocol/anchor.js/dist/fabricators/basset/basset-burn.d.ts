import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider';
interface Option {
    address: string;
    amount: string;
}
export declare const fabricatebAssetBurn: ({ address, amount }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
