import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { Expire } from '../types';
interface Option {
    address: string;
    amount: string;
    spender: string;
    expires?: Expire;
}
export declare const fabricatebAssetIncreaseAllowance: ({ address, amount, spender, expires }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
