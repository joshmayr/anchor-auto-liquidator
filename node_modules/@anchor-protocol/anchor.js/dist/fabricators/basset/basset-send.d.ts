import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    amount: string;
    contract: string;
    msg?: object;
}
export declare const fabricatebAssetSend: ({ address, amount, contract, msg }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
