import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    validator: string;
}
/**
 * @param address Validator Terra address.
 */
export declare const fabricatebAssetDeregisterValidator: ({ address, validator }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
