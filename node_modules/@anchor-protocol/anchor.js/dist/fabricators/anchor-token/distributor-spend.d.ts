import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    recipient: string;
    amount: string;
}
export declare const fabricateDistributorSpend: ({ address, recipient, amount }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
