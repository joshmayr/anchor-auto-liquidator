import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    distributor: string;
}
export declare const fabricateDistributorAddDistributor: ({ address, distributor }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
