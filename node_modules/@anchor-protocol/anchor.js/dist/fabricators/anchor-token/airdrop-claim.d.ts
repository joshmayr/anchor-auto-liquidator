import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    stage: number;
    amount: string;
    proof: string[];
}
export declare const fabricateAirdropClaim: ({ address, stage, amount, proof }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
