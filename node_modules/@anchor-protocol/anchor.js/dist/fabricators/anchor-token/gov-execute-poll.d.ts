import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    poll_id: number;
}
export declare const fabricateGovExecutePoll: ({ address, poll_id }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
