import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    spend_limit?: string;
}
export declare const fabricateDistributorUpdateConfig: ({ address, spend_limit }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
