import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
export interface ExecuteMsg {
    order: number;
    contract: string;
    msg: string;
}
interface Option {
    address: string;
    amount: string;
    title: string;
    description: string;
    link?: string;
    execute_msgs?: ExecuteMsg[];
}
export declare const fabricateGovCreatePoll: ({ address, amount, title, description, link, execute_msgs }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
