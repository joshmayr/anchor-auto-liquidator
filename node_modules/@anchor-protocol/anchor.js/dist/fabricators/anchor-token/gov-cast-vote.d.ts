import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
export declare type VoteOption = 'yes' | 'no';
interface Option {
    address: string;
    poll_id: number;
    vote: VoteOption;
    amount: string;
}
export declare const fabricateGovCastVote: ({ address, poll_id, vote, amount }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
