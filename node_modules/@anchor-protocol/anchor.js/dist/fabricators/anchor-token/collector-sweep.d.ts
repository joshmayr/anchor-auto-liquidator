import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    denom: string;
}
export declare const fabricateCollectorSweep: ({ address, denom }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
