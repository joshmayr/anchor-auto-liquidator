import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    merkle_root: string;
}
export declare const fabricateAirdropRegisterMerkleRoot: ({ address, merkle_root }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
