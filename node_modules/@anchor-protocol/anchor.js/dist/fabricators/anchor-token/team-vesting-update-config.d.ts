import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    owner?: string;
    anchor_token?: string;
    genesis_time?: string;
}
export declare const fabricateTeamVestingUpdateConfig: ({ address, owner, anchor_token, genesis_time }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
