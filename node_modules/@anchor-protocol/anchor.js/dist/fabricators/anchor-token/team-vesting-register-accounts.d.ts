import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    vesting_accounts: string[];
}
export declare const fabricateTeamVestingRegisterAccounts: ({ address, vesting_accounts }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
