import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    reward_factor?: string;
}
export declare const fabricateCollectorUpdateConfig: ({ address, reward_factor }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
