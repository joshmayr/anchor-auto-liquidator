import { AddressProvider } from '../../address-provider/provider';
import { MsgExecuteContract } from '@terra-money/terra.js';
/**
 * @param address Client’s Terra address (address of the message sender).
 * @param Client’s Terra address (address of beth token contract).
 */
interface Option {
    address: string;
    token_contract?: string;
}
export declare const fabricatebEthPostInit: ({ address, token_contract }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
