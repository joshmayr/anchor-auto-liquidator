import { AddressProvider } from '../../address-provider/provider';
import { MsgExecuteContract } from '@terra-money/terra.js';
/**
 * @param address Client’s Terra address (address of the message sender).
 * @param Client’s Terra address (address of reward recipient).
 */
interface Option {
    address: string;
    recipient?: string;
}
export declare const fabricatebEthClaimRewards: ({ address, recipient }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
