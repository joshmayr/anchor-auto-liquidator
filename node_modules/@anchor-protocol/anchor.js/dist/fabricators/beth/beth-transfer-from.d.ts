import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
/**
 * @param address Client’s Terra address (address of the message sender).
 * @param amount to send.
 * @param recipient Client's Terra address (address of recipient).
 * @param owner Client;s terra address (owner of the allowance).
 */
interface Option {
    address: string;
    amount: string;
    owner: string;
    recipient: string;
}
export declare const fabricatebEthTransferFrom: ({ address, amount, owner, recipient }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
