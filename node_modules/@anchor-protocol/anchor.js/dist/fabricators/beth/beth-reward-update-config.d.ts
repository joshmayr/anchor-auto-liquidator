import { AddressProvider } from '../../address-provider/provider';
import { MsgExecuteContract } from '@terra-money/terra.js';
/**
 * @param address Client’s Terra address (address of the message sender).
 * @param Client’s Terra address (address of the new owner).
 */
interface Option {
    address: string;
    owner?: string;
}
export declare const fabricatebEthUpdateConfig: ({ address, owner }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
