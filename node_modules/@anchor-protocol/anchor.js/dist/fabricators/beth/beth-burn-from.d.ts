import { AddressProvider } from '../../address-provider/provider';
import { MsgExecuteContract } from '@terra-money/terra.js';
/**
 * @param address Client’s Terra address (address of the message sender).
 * @param amount of burn.
 * @param owner Client's Terra address (address of allowance owner).
 */
interface Option {
    address: string;
    amount: string;
    owner: string;
}
export declare const fabricatebEthBurnFrom: ({ address, amount, owner }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
