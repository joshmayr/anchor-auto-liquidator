import { MsgExecuteContract } from '@terra-money/terra.js';
declare type Expire = {
    at_height: number;
} | {
    at_time: number;
} | {
    never: {};
};
interface Option {
    address: string;
    amount: string;
    contract_address: string;
    spender: string;
    expires?: Expire;
}
export declare const fabricateCw20IncreaseAllowance: ({ address, amount, contract_address, spender, expires, }: Option) => MsgExecuteContract[];
export {};
