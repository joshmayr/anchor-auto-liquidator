import { MsgExecuteContract } from '@terra-money/terra.js';
interface Option {
    address: string;
    contract_address: string;
    amount: string;
    recipient: string;
}
export declare const fabricateCw20Transfer: ({ address, contract_address, amount, recipient, }: Option) => MsgExecuteContract[];
export {};
