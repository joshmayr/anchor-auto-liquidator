import { MsgExecuteContract } from '@terra-money/terra.js';
interface Option {
    address: string;
    contract_address: string;
    amount: string;
    contract: string;
    msg?: object;
}
export declare const fabricateCw20Send: ({ address, contract_address, amount, contract, msg, }: Option) => MsgExecuteContract[];
export {};
