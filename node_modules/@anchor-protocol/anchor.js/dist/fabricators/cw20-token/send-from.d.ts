import { MsgExecuteContract } from '@terra-money/terra.js';
interface Option {
    address: string;
    contract_address: string;
    amount: string;
    owner: string;
    contract: string;
    msg?: object;
}
export declare const fabricateCw20SendFrom: ({ address, contract_address, amount, contract, owner, msg, }: Option) => MsgExecuteContract[];
export {};
