import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    amount: string;
    to?: string;
    belief_price?: string;
    max_spread?: string;
}
export declare const fabricateExchangeSwapANC: ({ address, amount, to, belief_price, max_spread }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
