import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
import { Expire } from '../types';
interface Option {
    address: string;
    token_amount: string;
    native_amount: string;
    quote: string;
    slippage_tolerance?: string;
    expires?: Expire;
}
export declare const fabricateExchangeProvideLiquidityANC: ({ address, slippage_tolerance, token_amount, native_amount, quote, expires, }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
