import { MsgExecuteContract } from '@terra-money/terra.js';
import { AddressProvider } from '../../address-provider/provider';
interface Option {
    address: string;
    amount: string;
}
export declare const fabricateExchangeWithdrawLiquiditybLuna: ({ address, amount }: Option) => (addressProvider: AddressProvider) => MsgExecuteContract[];
export {};
