import { LCDClient } from '@terra-money/terra.js';
import { AddressProvider, MARKET_DENOMS } from '../../address-provider';
import { fabricateMarketDepositStableCoin, fabricateMarketRedeemStable, OmitAddress, OptionType } from '../../fabricators';
import { Operation } from '../operation';
export declare type EarnDepositStableOption = OmitAddress<OptionType<typeof fabricateMarketDepositStableCoin>>;
export declare type EarnWithdrawStableOption = OmitAddress<OptionType<typeof fabricateMarketRedeemStable>>;
export interface GetTotalDepositOption {
    market: MARKET_DENOMS;
    address: string;
}
export interface GetApyOption {
    market: MARKET_DENOMS;
}
export declare class Earn {
    private _lcd;
    private _addressProvider;
    constructor(lcd: LCDClient, addressProvider: AddressProvider);
    depositStable(depositStableOption: EarnDepositStableOption): Operation;
    withdrawStable(withdrawStableOption: EarnWithdrawStableOption): Operation;
    getTotalDeposit(getTotalDepositOption: GetTotalDepositOption): Promise<string>;
    getAPY(getAPYOption: GetApyOption): Promise<number>;
}
